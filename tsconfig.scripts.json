{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "NodeNext",        // ← Solo para scripts
    "moduleResolution": "NodeNext" // ← Solo para scripts
  },
  "include": ["scripts/**/*.ts"]
}